import React from 'react';
import ReactDOM from 'react-dom/client';

import App from "./Hooks/useReducer2";
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <div>
        <App/>
    </div>
);
