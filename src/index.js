import React from 'react';
import ReactDOM from 'react-dom/client';

import App from "./Hooks/useReducer3";
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <div>
        <App/>
    </div>
);
