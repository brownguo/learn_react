import React from 'react';
import ReactDOM from 'react-dom/client';

import Index from "./PubSub/PubSub";
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <div>
        <Index/>
    </div>
);
